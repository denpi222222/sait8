# Netlify redirects for static files
/images/* /images/:splat 200
/public/* /public/:splat 200
/icons/* /icons/:splat 200

# Handle static assets
/*.png /:splat 200
/*.jpg /:splat 200
/*.jpeg /:splat 200
/*.gif /:splat 200
/*.svg /:splat 200
/*.ico /:splat 200

# API routes
/api/* /.netlify/functions/api/:splat 200

# SPA fallback
/* /index.html 200 